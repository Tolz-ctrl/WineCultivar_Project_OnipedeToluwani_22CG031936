# Wine Cultivar Prediction System - Dependencies
# Pinned for Python 3.11 compatibility with prebuilt wheels on Render

# Web Framework
Flask==3.0.3
# Flask 3.0.3 has prebuilt wheels for Python 3.11 and is stable for production

Werkzeug==3.0.3
# Werkzeug 3.0.3 is compatible with Flask 3.0.3 and has prebuilt wheels

# WSGI Server for Production
gunicorn==21.2.0
# Gunicorn 21.2.0 has prebuilt wheels for Python 3.11

# Machine Learning - CRITICAL: Use versions with prebuilt wheels for Python 3.11
numpy==1.24.3
# numpy 1.24.3 has prebuilt wheels for Python 3.11 (avoids Cython compilation)
# Must install BEFORE pandas and scikit-learn

pandas==2.0.3
# pandas 2.0.3 has prebuilt wheels for Python 3.11 (depends on numpy 1.24.x)
# Avoids source build and Cython errors

scikit-learn==1.3.2
# scikit-learn 1.3.2 has prebuilt wheels for Python 3.11
# Compatible with numpy 1.24.3 and scipy 1.11.x

scipy==1.11.4
# scipy 1.11.4 has prebuilt wheels for Python 3.11
# Required by scikit-learn, avoids compilation

# Model Persistence
joblib==1.3.2
# joblib 1.3.2 has prebuilt wheels for Python 3.11

